# Conexión al repositorio
backend:
  name: github
  repo: UlisesGutierrez2/piloto-cms
  branch: main

# Flujo de trabajo editorial (Pull Requests)
local_backend: true
publish_mode: editorial_workflow

# Configuración global de medios (Fallback)
media_folder: "static/videos"
public_folder: "/videos"

collections:
  # --- 1. COLECCIÓN: ONBOARDING ---
  - name: "onboarding"
    label: "Onboarding"
    label_singular: "Documento de Onboarding"
    folder: "docs/onboarding"
    create: true
    extension: "md"
    format: "frontmatter"
    nested:
      depth: 10
    meta:
      path:
        widget: string
        label: '📂 Directorio de guardado (Opcional)'
        index_file: 'index'
    
    # Almacén específico para Onboarding (Alineado con producción)
    media_folder: "static/videos/onboarding"
    public_folder: "/videos/onboarding"
    
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - label: "Cuerpo del texto"
        name: "body"
        widget: "markdown"
        buttons: ["bold", "italic", "code", "link", "heading-two", "heading-three", "quote", "bulleted-list", "numbered-list"]
        editor_components: ["image-renderer-component", "video"]

  # --- 2. COLECCIÓN: LINEAMIENTOS ---
  - name: "lineamientos"
    label: "Lineamientos"
    label_singular: "Documento de Lineamientos"
    folder: "docs/lineamientos"
    create: true
    extension: "md"
    format: "frontmatter"
    nested:
      depth: 10
    meta:
      path:
        widget: string
        label: '📂 Directorio de guardado (Opcional)'
        index_file: 'index'

    # Almacén específico para Lineamientos
    media_folder: "static/videos/lineamientos"
    public_folder: "/videos/lineamientos"
    
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - label: "Cuerpo del texto"
        name: "body"
        widget: "markdown"
        buttons: ["bold", "italic", "code", "link", "heading-two", "heading-three", "quote", "bulleted-list", "numbered-list"]
        editor_components: ["image-renderer-component", "video"]
